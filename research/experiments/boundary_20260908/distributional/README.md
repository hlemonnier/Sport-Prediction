# Paused distributional experiment — unfitted draft

Status: paused before any model fitting or distributional score inspection because the user redirected work to integration, commit and push. The specification and inherited-input design lock are preserved. `model.py` and `run.py` are incomplete, untested research drafts, not validated implementation or production candidates.

The intended experiment predicts conditional residual quantiles around the frozen F1 HGB point forecast and compares weighted interval scores with global and conditional empirical quantile references on the original eligible population. It would preserve the point forecast exactly; no point-accuracy improvement is claimed. This is custom quantile HGB, not an implementation of NGBoost.

There are no selected candidates, fitted quantile models, performance results, completed tests, transfer scores, or promotion evidence. The source and artifact hashes plus remaining verification tasks are recorded in `artifacts/research/boundary_20260908/distributional/status.json`. The draft transfer-input hash reader still needs validation against the actual inherited manifest schema before execution. Do not interpret the available command-line entry point as execution readiness.

If research is resumed, finish mathematical and causal tests, validate all inherited input contracts, add an independent verifier, and freeze tested source hashes before running the prespecified chronological selection. Follow the frozen advancement gate before reading any transfer scores. Large inherited input tables and fitted point-model caches are local research dependencies and are not supplied by these draft files.

Suggested commit: `research: preserve paused distributional forecast protocol and draft`
