# Sport Prediction Web

Next.js UI for the local sport prediction system.

## Install & Run

```bash
cd apps/web
pnpm install
pnpm dev
```

Env options (optional):
- `NEXT_PUBLIC_API_URL` (default http://localhost:4000)
