# Sport Prediction Web

Next.js UI for the local quant research platform.

## Install & Run

```bash
cd platform/web
pnpm install
pnpm dev
```

Env options (optional):
- `NEXT_PUBLIC_API_URL` (default http://localhost:4000)
