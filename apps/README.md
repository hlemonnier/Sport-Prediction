# Apps

Application entrypoints for the sport prediction system.

- `apps/api` contains the local API implementation and is the default backend start path.
- `apps/web` contains the local web UI implementation and is the default web start path.
